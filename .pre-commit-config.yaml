repos:
  - repo: local
    hooks:
      - id: format-py-files
        name: format files
        entry: bash
        language: system
        files: \.(py)$
        pass_filenames: false
        args:
          - -c
          - "fd -e py -x ruff format -q"
